function variaveis(){
    let numero1 = 6;
    let numero2 = 7; 
    let diferenca;
    let B; 
    let mult;

    diferenca = numero1 - numero2;
    B = (numero1 * 2) + (numero2 * 3);
    mult = numero1*numero2;

    alert ("A diferença entre os 2 números é: " + diferenca);
    alert ("O dobro do primeiro número somado ao triplo do segundo é: " + B);
    alert ("A multiplicação dos 2 números é: " + mult);

}